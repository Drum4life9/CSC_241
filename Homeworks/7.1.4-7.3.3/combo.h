#ifndef COMBO_H__
#define COMBO_H__
#include <string>

int combine(int a, int b);
std::string combine(const std::string& a, const std::string& b);
std::string combine(const std::string& a, int b);

#endif /* COMBO */
